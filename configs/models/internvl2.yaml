# InternVL2 model registry — stub entries.
#
# This file is intentionally pinned to `adapter_kind: stub` so the unit
# tests, smoke tests, and CPU-only local pipeline keep working without
# torch installed. For real GPU runs (Colab Pro etc.) point your run
# config at `configs/models/internvl2_real.yaml` instead.
models:
  internvl2-2b:
    adapter_kind: stub
    model_id: OpenGVLab/InternVL2-2B
    notes: "Small model baseline; stub for local dev. Real adapter lives in internvl2_real.yaml."
  internvl2-8b:
    adapter_kind: stub
    model_id: OpenGVLab/InternVL2-8B
    notes: "Upper-bound matched-cost baseline; stub until real 8B adapter is wired."
